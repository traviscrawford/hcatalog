<?xml version="1.0"?>

<!--
   Licensed to the Apache Software Foundation (ASF) under one or more
   contributor license agreements.  See the NOTICE file distributed with
   this work for additional information regarding copyright ownership.
   The ASF licenses this file to You under the Apache License, Version 2.0
   (the "License"); you may not use this file except in compliance with
   the License.  You may obtain a copy of the License at

       http://www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS,
   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   See the License for the specific language governing permissions and
   limitations under the License.
-->

<project xmlns:ivy="antlib:org.apache.ivy.ant">

  <!--
     To avoid Ivy leaking things across big projects, always load Ivy in the same classloader.
     Also note how we skip loading Ivy if it is already there, just to make sure all is well.
  -->
  <target name="ivy-init-antlib" depends="ivy-download,ivy-init-dirs,ivy-probe-antlib" unless="ivy.found">
    <typedef uri="antlib:org.apache.ivy.ant" onerror="fail" loaderRef="ivyLoader">
      <classpath>
        <pathelement location="${ivy.jar}"/>
      </classpath>
    </typedef>
    <fail>
      <condition >
        <not>
          <typefound uri="antlib:org.apache.ivy.ant" name="cleancache"/>
        </not>
      </condition>
         You need Apache Ivy 2.0 or later from http://ant.apache.org/
         It could not be loaded from ${ivy_repo_url}
    </fail>
  </target>

  <target name="ivy-init-dirs">
    <mkdir dir="${build.ivy.dir}"/>
    <mkdir dir="${build.ivy.report.dir}"/>
  </target>

  <target name="ivy-probe-antlib" >
    <condition property="ivy.found">
      <typefound uri="antlib:org.apache.ivy.ant" name="cleancache"/>
    </condition>
  </target>

  <target name="ivy-download" description="To download ivy" unless="offline">
    <get src="${ivy_repo_url}" dest="${ivy.jar}" usetimestamp="true" skipexisting="true"/>
  </target>

  <target name="ivy-init" depends="ivy-init-antlib" unless="ivy-init.complete">
    <echo message="${ant.project.name}"/>
    <ivy:configure file="${ivysettings.xml}"/>
    <ivy:makepom ivyfile="${ivy.xml}" pomfile="${pom.file}"/>
    <property name="ivy-init.complete" value="true"/>
  </target>

  <target name="ivy-retrieve" depends="ivy-init" description="Retrieve Ivy dependencies">
    <echo message="${ant.project.name}"/>
    <ivy:retrieve sync="true"
      pattern="${build.dir}/ivy/[conf]/[organization]-[artifact]-[revision](-[classifier]).[ext]"/>
    <ivy:cachepath pathid="ivy.default.classpath" conf="default"/>
    <ivy:cachepath pathid="ivy.test.classpath" conf="test"/>
  </target>

  <target name="ivy-report" depends="ivy-retrieve" description="Generate Ivy dependency report">
    <ivy:report todir="${build.ivy.report.dir}" graph="false"/>
  </target>

</project>
